# Leave VITE_API_URL blank for same-origin hosting through /api/v1.
# Set it only when the API is hosted on a different origin.
VITE_API_URL=
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
VITE_LIVE_REFRESH_MS=30000
